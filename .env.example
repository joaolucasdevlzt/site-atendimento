# Base URL da API (usada pela instância Axios).
# No modo apresentação isto não é necessário — os serviços usam dados mock.
VITE_API_URL=http://localhost:3333

# Alterne para "false" quando tiver um backend real rodando.
VITE_USE_MOCK=true
